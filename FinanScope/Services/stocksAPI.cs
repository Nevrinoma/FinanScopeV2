using System;
using System.Collections.Generic;
using System.IO;
using System.Net;
using System.Text;
using System.Xml.Linq;

namespace FinanScope.Services
{
    internal class stocksAPI
    {
        //public static List<MyStocks> GetRandomRecipes()
        //{
        //    string url = $"";
        //    WebRequest request = WebRequest.Create(url);
        //    request.Headers["X-RapidAPI-Host"] = "";
        //    request.Headers["X-RapidAPI-Key"] = "";
        //    request.Method = "GET";
        //    request.ContentType = "application/json";
        //    WebResponse response = (HttpWebResponse)request.GetResponse();
        //    using (var reader = new StreamReader(response.GetResponseStream()))
        //    {
        //        JObject recipeData = JObject.Parse(reader.ReadToEnd());
        //        JArray recipes = (JArray)recipeData[""];


        //        foreach (JToken recipe in recipes)
        //        {

        //        }

        //    }

        //}
    }
}
